# Core dependencies
pystache>=0.6.0
markdown>=3.4.0
beautifulsoup4>=4.12.0
pytz>=2023.3
watchdog>=3.0.0

# Settings loader (Python < 3.11 only; 3.11+ uses built-in tomllib)
tomli>=2.0.1; python_version < '3.11'

# Testing dependencies (optional, for development)
pytest>=7.4.0
pytest-asyncio>=0.21.0
