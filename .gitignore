# Allowlist approach: ignore everything, then explicitly permit public files.
# Any new file in yak-steel is automatically excluded from the public repo.
# To add a file to the public repo, add a !path line below.

# Ignore everything by default
*

# Allow git config
!.gitignore

# Allow the yakbarber package
!yakbarber/
!yakbarber/**

# Allow the backward-compatible wrapper
!yakbarber3.py

# Allow the example settings (placeholder values only)
!settings.example.toml

# Allow templates (templatized, no personal data)
!templates/
!templates/default/
!templates/default/**

# Allow the test suite
!tests/
!tests/**
!tests/fixtures/
!tests/fixtures/**

# Still ignore compiled Python everywhere
__pycache__/
*.pyc
*.pyo

# Still ignore OS files everywhere
.DS_Store
